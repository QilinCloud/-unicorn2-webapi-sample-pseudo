# Samples Legacy Landscape

This sample follows the idea of the old `syncExample.php`: every method contains enough pseudo marketplace code to make the expected response shape understandable.

The implementation has been updated to the current ApiWeb protocol and current interface/enumeration classes.

