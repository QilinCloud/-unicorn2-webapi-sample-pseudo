# Pseudo Implementation Overview

This folder contains the executable pseudo marketplace implementation.

`sync.php` implements every supported ApiWeb method with deterministic example data. Replace the marked pseudo sections with calls to the real marketplace API.

