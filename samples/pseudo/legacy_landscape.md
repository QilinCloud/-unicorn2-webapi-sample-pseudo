# Pseudo Implementation Legacy Landscape

Marc's old `syncExample.php` used simple method bodies to show the expected structure to third-party developers.

This implementation keeps that learning style, but updates the request authentication, response shape, enum usage, error handling and capability metadata to the current ApiWeb connector contract.

