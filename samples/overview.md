# Samples Overview

This folder contains the marketplace-facing implementation variants for this standalone sample repository.

In this repository only `pseudo/` is active. It contains executable pseudo marketplace code and deliberately marks the places where a real marketplace SDK or HTTP client should be called.

