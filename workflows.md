# Workflows

1. Start the sample with `php -S 127.0.0.1:18080 -t .`.
2. Confirm `validateCredentials` and `getCapabilities` from Unicorn.
3. Replace one pseudo method at a time with real marketplace API calls.
4. Keep negative tests in place while adding real transport.
