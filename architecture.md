# Architecture

`api.php` handles the signed ApiWeb envelope. `sync.php` loads protocol helpers.
`samples/pseudo/sync.php` contains every method body that should be replaced by
real marketplace API calls.
