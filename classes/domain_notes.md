# ApiWeb Sample Classes Domain Notes

These classes are sample infrastructure, not business logic. Replace the pseudo blocks in `samples/pseudo/sync.php` with real marketplace API calls.

Keep runtime changes compatible with PHP 8.x and avoid extensions beyond `json`, `hash`, and `openssl`.
